{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd To_Do_controls && python manage.py collectstatic --noinput && gunicorn To_Do_controls.wsgi:application --bind 0.0.0.0:$PORT"
  }
}